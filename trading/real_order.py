# trading/real_order.py

def place_real_order(symbol: str, action: str, price: float, amount: float):
    """
    📢 اجرای سفارش واقعی (CoinEx)
    توجه: این بخش باید با API واقعی و احراز هویت پیاده‌سازی شود.
    """
    raise NotImplementedError("🛑 سفارش واقعی فعلاً فعال نیست. لطفاً در حالت SIMULATION تست کنید.")
